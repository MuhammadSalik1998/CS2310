#include <iostream>
using namespace std;
/*
int main1(){

    int arr_in[20] = {0};
    int arr_out[20] = {0};
    int size;

    cout<<"Enter the size of the array: ";
    cin>>size;

    cout<<"Enter the elements of the array: ";
    for(int i=0; i<size; i++){
        cin>>arr_in[i];
    }
    int max;
    for(int i =0; i<size; i++){
        max = -1;
        for (int j=i+1; j<size; j++){
            if(arr_in[i]>arr_in[j]){
                if(arr_in[j]>max){
                    max=arr_in[j];
                }
            }
        }
        arr_out[i]=max;
    }

    for (int i=0; i<size; i++){
        cout<<"Output is: "<<arr_out[i]<<endl;
    }
}
*/
